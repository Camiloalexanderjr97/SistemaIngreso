{{>navigations}}
<div class="wrapper d-flex align-items-stretch">
    {{>NavLat}}
    <!-- Page Content  -->
    <div id="content" class="p-4 p-md-5 pt-5">
        <link rel="stylesheet" type="text/css" href="../../CSS/registro.css" />
        <center>
            <h2 class="mb-4 ">Registro</h2>
        </center>
        <div class="wrapper">


            <form class="login" action="/links/Admin/RegistroCredencial" method="POST">
                <p class="title">Credencial</p>
                <input type="number"  maxlength="12"   oninput="if(this.value.length > this.maxLength) this.value = this.value.slice(0, this.maxLength);"  name="Cedula" placeholder="Cedula" autofocus required/>
                <i class="fa fa-user"></i>
                <input type="text" name="usuario" placeholder="Nickname" required/>
                <i class="fa fa-user"></i>
                <input type="text" name="Password" placeholder="Password" required/>
                <i class="fa fa-key"></i>


                <button>
                    <i class="spinner"></i>
                    <span class="state">Crear</span>
                </button>
            </form>

        </div>

    </div>
</div>

{{>script}}